# Claude Agent Team — Python dependencies
# Python 3.10+ required (3.13 recommended)

# Anthropic Claude (subscription via Claude Code CLI auth, no API key needed)
claude-agent-sdk>=0.1.80

# Multi-provider LLM routing (OpenAI, Google, Mistral, Groq, Cerebras, Together,
# DeepSeek, xAI, Cohere, Perplexity, OpenRouter, Fireworks, NVIDIA, HF, etc.)
litellm>=1.83.0

# .env file loading
python-dotenv>=1.0.0
